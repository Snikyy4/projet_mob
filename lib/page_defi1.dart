
import 'package:flutter/material.dart';

class Defi1Screen extends StatelessWidget {
  const Defi1Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Jeu des drapeaux'),
      ),
      
    );
  }
}